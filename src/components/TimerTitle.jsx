export const TimerTitle = ({ title }) => {
  return (
    <div className="row-start-2 mt-4 flex items-end justify-center">
      <h1 className="font-serif text-6xl font-bold">{title}</h1>
    </div>
  );
};
